[tool.pytest.ini_options]
# `pythonpath = ["."]` lets tests do `from app... import ...` without installing
# the project. `testpaths` keeps `pytest` (no args) scoped to the tests dir.
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
