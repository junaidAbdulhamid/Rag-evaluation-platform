# Deploy set — exactly what the Streamlit dashboard (dashboard/app.py) imports.
# Kept lean so `streamlit run` deploys (Streamlit Community Cloud, Render, ...) stay
# small and fast. The full RAG pipeline needs more — see requirements-pipeline.txt.
streamlit>=1.30
pydantic>=2.7
pydantic-settings>=2.2
numpy>=1.26
